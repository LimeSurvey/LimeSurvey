{#
/**
 * Array header : answer or sub question
 * @var $class
 * @var $content
 * @var $basename
 * @var $code
 */
#}
<th class="{{ class }}" id="answer{{ basename }}-{{ code }}" role="columnheader">
    {{ content }}
</th>