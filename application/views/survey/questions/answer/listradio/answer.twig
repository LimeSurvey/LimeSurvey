{#
/**
 * List Radio Html
 *
 * @var $name
 * @var $value
 */
#}

<!-- List Radio -->

<!-- answer -->
{{ sTimer }}
<ul class="{{ coreClass }} list-unstyled" role="radiogroup" aria-labelledby="ls-question-text-{{ name }}" data-column-count={{iNbCols}}>
    {# rows/answer_row.php #}
    {{ sRows }}
</ul>
{# Value for expression manager javascript (use id) ; no need to submit #}
{{ C.Html.hiddenField("java"~name,value,({
    'id' : "java"~name,
    'disabled' : true,
}))
}}
<!-- end of answer -->
 
