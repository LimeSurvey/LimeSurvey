import { shallowMount } from '@vue/test-utils'
import QuestionOverview from '@/components/questionoverview.vue'

describe('questionoverview.vue', () => {
  it('renders props.msg when passed', () => {
    //const wrapper = shallowMount(QuestionOverview);
    //expect(wrapper.find('#QuestionEditor--test--questionoverviewcontainer').exists()).toBe(true)
  })
})
