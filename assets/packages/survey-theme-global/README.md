# How to build

Run `gulp build_survey_theme_global` from repository root.

If you need the changes to appear in `themes/survey/fruity_twentythree`, also run `gulp build_survey_theme_ls6`, because that task merges the global package into the LS6 variation bundles.
