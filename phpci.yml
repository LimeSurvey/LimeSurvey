build_settings:
    clone_depth: 1 # depth of 1 is a shallow clone, remove this line to clone entire repo
    verbose: true
    ignore:
        - "application/vendor"
        - "tests"
        - "third_party"
        - "application/third_party"
        - "locale"
    mysql:
        host: "localhost"
        user: "root"
        pass: ""

setup:
#    mysql:
#        - "DROP DATABASE IF EXISTS test;"
#        - "CREATE DATABASE test;"
#        - "GRANT ALL PRIVILEGES ON test.* TO test@'localhost' IDENTIFIED BY 'test';"
    composer:
        action: "install"

test:
    php_parallel_lint: 
        allow_failures: false
#    php_mess_detector:
#        allow_failures: true
#        suffixes:
#            - "php"
#        rules:
#            - "unusedcode"
#    php_unit:
#        config:
#            - "PHPUnit-all.xml"
#            - "PHPUnit-ubuntu-fix.xml"
#        directory:
#            - "tests/"
#        run_from: "phpunit/"
#        coverage: "tests/logs/coverage"
#    php_mess_detector:
#        allow_failures: false
#        suffixes:
#            - "php"
#    php_code_sniffer:
#        standard: "PSR2"
    php_cpd:
        allow_failures: true
#    grunt:
#        task: "build"
    php_docblock_checker:
        allow_failures: true
    php_loc:
        
    

complete:
#    mysql:
#        - "DROP DATABASE IF EXISTS test;"
success:
    shell: 
        
    git:
        "2.07":
            push: 
                remote: "git@github.com:SamMousa/Releases.git"
                branch: "master"