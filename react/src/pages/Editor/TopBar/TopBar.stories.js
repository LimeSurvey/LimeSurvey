import React from 'react'

import { TopBar } from './TopBar'

export default {
  title: 'Page/Editor/TopBar',
  component: TopBar,
}

export const Basic = () => (
  <div id="container">
    <TopBar />
  </div>
)
