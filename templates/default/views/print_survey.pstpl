<!DOCTYPE html>
<html {HTMLTAG}>
    <head>
        <title>{SITENAME} - {SURVEYNAME}</title>
        {HEADELEMENTS}
    </head>
    <body>
        <div id="limesurvey" class="container">
<!--
            <div class="navbar navbar-default navbar-fixed-top">
                <div class="navbar-header ">
                <div class="navbar-brand">{SURVEYNAME}</div>
                </div>
            </div>
-->
            <h1>{SURVEYNAME}</h1>
            <div class="survey-description">{SURVEYDESCRIPTION}</div>

            <div class="survey-welcome">{WELCOME}</div>

            <p class="x-questions">{THEREAREXQUESTIONS}</p>
            {GROUPS}
            {PRIVACY}
            <div class="submit">
                <div class="submit-end">{END}</div>
                <div class="submit-submitby">{SUBMIT_BY}</div>
                <div class="submit-faxto">{FAX_TO}</div>
                <div class="submit-submitext">{SUBMIT_TEXT}</div>
                <div class="submit-submitthanks">{THANKS}</div>
            </div>
        </div>
    </body>
</html>
