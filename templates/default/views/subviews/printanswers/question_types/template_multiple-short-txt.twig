 <div class="col-md-8 child-row-spacing">
    {% for subquestionId, subquestion in question.subquestions %}
        <div class="row">
            <div class="col-md-8">
                {{subquestion.answervalue}}
            </div>
            <div class="col-md-4 text-left">
                <b>{{subquestion.question}} ({{subquestion.qid}})</b>
            </div>
        </div>
    {% endfor %}
</div>
