<?php

if (!defined('BASEPATH')) {
    exit('No direct script access allowed');
}

/**
 * Translatable strings from the config.xml file in this directory.
 *
 * The theme title and description, and the help texts, captions and option labels of its question
 * attributes are translated at runtime with gT(), but the translation script can't pick them up from XML,
 * so they are listed here.
 * This file has no functionality except for being searchable by the translation script.
 * When adding or changing such a text in config.xml, update the matching entry here.
 */
gT("Bootstrap dropdown");
gT('Enable search function');
gT('Allow searching of dropdown');
gT('On (searchable)');
gT('Off (no search function)');
gT('Show a tick on the selected item in the dropdown');
gT('Display tick on selected item');
gT('Show tick');
gT('No tick');
gT('Can be as wide as the entry that is selected, or fixed at the longest entry (default)');
gT('Width of dropdown should be the currently selected entry');
gT('Width of dropdown to be currently selected entry');
gT('Width of dropdown to be longest entry');
