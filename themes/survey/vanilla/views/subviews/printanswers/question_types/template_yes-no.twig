 {% extends "./subviews/printanswers/printanswers_question.twig" %}
 {% block question_answer %}
 <div class="col-lg-8">
    <div class="row">
        <div class="col-lg-4 text-end">
        </div>
        <div class="col-lg-8 text-start">
            {% if question.answervalue is sameas("Y") %}
                {{ gT("Yes") }} <i class="fa fa-check ri-check-fill"></i>
            {% elseif question.answervalue is sameas("N") %}
                {{ gT("No") }} <i class="fa fa-close ri-close-fill"></i>
            {% else %}
                {{question.answervalue}}
            {% endif %}
        </div>
    </div>
</div>
{% endblock %}

